# ToneBridge - 선택적 패키지들 (향상된 기능)
# 환경에 따라 설치가 어려울 수 있는 패키지들

# ============ 추가 STT 엔진 ============
# OpenAI Whisper (폴백용)
openai-whisper>=20240930
# PyTorch (Whisper 의존성)
torch>=2.0.0
# PyTorch 오디오
torchaudio>=2.0.0

# ============ 고급 음성 처리 ============
# 음성 활동 감지 (VAD)
webrtcvad>=2.0.10
# TextGrid 파일 처리
textgrid>=1.6.1
# 문자열 유사도 계산
python-Levenshtein>=0.26.0

# ============ 시각화 및 분석 ============
# 그래프 생성
matplotlib>=3.7.0
# 데이터 분석
pandas>=2.0.0
# 이미지 처리
Pillow>=10.0.0

# ============ 성능 향상 ============
# JIT 컴파일러 (librosa 가속)
numba>=0.58.0
# LLVM 바인딩 (numba 의존성)
llvmlite>=0.42.0

# 설치 참고사항:
# - 이 패키지들은 시스템 환경에 따라 설치가 실패할 수 있습니다
# - 기본 기능 동작에는 필수가 아닙니다
# - 개별적으로 필요에 따라 설치하세요